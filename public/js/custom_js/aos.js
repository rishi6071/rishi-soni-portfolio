// Animate on Scroll Library Initialisation
(() => {
    AOS.init({
        startEvent: 'DOMContentLoaded',
        once: true,
        delay: 100,
        easing: "ease",
        duration: 1000
    });
})()